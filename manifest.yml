version: "3.0"
description: Test manifest
repository: https://github.com/
hydraVersion: "4"

typegen:
  metadata:
    source: indexer/types.json
  events:
    # - assets.Transfer
  calls:
    - timestamp.set
  outDir: chain
  # customTypes:
  #   typedefsLoc:

mappings:
  mappingsModule: mappings
  eventHandlers:
  #   - event: assets.transfer
  #     handler: transfersHandler
  # extrinsicHandlers:
  # preBlockHooks:
  # postBlockHooks:

  extrinsicHandlers:
    - extrinsic: assets.transfer
      handler: transfersHandler
      # triggerEvents: # optional list of events triggering the execution
      #   - system.ExtrinsicSuccess
      #   - system.ExtrinsicFailure
